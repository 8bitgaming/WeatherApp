import React, { PureComponent } from 'react'

export default class DrivingCheck extends PureComponent {
    render() {
        return (
            <div>
                <button>Driving Check</button>
            </div>
        )
    }
}
